@model FrontEnd.Models.TiposViewModel

@{
    ViewBag.Title = "Details";
}
    <div class="container">
        <h2>Details</h2>

        <div>
            <h4>TiposViewModel</h4>
            <hr />
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.vNombre)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.vNombre)
                </dd>

            </dl>
        </div>
        <p>
            @Html.ActionLink("Edit", "Edit", new { id = Model.idTipo }) |
            @Html.ActionLink("Back to List", "Index")
        </p>
    </div>